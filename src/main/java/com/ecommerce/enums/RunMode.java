package com.ecommerce.enums;

/**
 * Supported execution environments (Local, Selenium Grid, Sauce Labs, BrowserStack).
 */
public enum RunMode {
    LOCAL,
    GRID,
    BROWSERSTACK,
    SAUCELABS
}
