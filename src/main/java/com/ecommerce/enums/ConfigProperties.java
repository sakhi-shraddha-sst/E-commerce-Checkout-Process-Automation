package com.ecommerce.enums;

/**
 * Key identifiers corresponding to entries in config.properties.
 */
public enum ConfigProperties {
    URL,
    BROWSER,
    RUNMODE,
    GRID_URL,
    BROWSERSTACK_URL,
    BROWSERSTACK_USERNAME,
    BROWSERSTACK_ACCESSKEY,
    SAUCELABS_URL,
    SAUCELABS_USERNAME,
    SAUCELABS_ACCESSKEY,
    HEADLESS,
    EXPLICIT_WAIT,
    OVERRIDE_REPORTS
}
